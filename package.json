{
  "name": "ai-capsule",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "npm install --include=dev --prefix client && npm run build --prefix client && npm install --prefix server",
    "start": "node server/server.js",
    "dev:server": "node server/server.js",
    "dev:client": "npm run dev --prefix client"
  }
}
